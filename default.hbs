<!DOCTYPE html>
<html lang="{{@site.locale}}">
<head>

    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1">    
    <link rel="stylesheet" type="text/css" href="{{asset "built/latex.css"}}" />
    <link rel="stylesheet" type="text/css" href="{{asset "built/screen.css"}}" />
    <link rel="stylesheet" type="text/css" href="{{asset "built/katex.min.css"}}" />
    <link rel="stylesheet" type="text/css" href="{{asset "built/copy-tex.min.css"}}" />
    <link rel="stylesheet" type="text/css" href="{{asset "built/prism-xonokai.css"}}" />

    <title>{{meta_title}}</title>

    {{ghost_head}}
    {{!-- Outputs important meta data and settings, should always be in <head> --}}

</head>
<body class="{{body_class}}">

<div class="gh-head-actions">
            <div class="gh-head-actions-list">
                {{#if @site.facebook}}
                <a href="{{facebook_url @site.facebook}}" title="Facebook" target="_blank" rel="noopener">{{> "icons/facebook"}}</a>
                {{/if}}
                {{#if @site.twitter}}
                <a href="{{twitter_url @site.twitter}}" title="Twitter" target="_blank" rel="noopener">{{> "icons/twitter"}}</a>
                {{/if}}
                <a href="https://feedly.com/i/subscription/feed/{{@site.url}}/rss/" title="RSS" target="_blank" rel="noopener">{{> "icons/rss"}}</a>
            </div>
        </div>

<header>
    <h1>
        <a class="gh-head-logo" href="{{@site.url}}">
            {{#if @site.logo}}
                <img src="{{@site.logo}}" alt="{{@site.title}}" />
            {{else}}
                {{@site.title}}
            {{/if}}
        </a>
    </h1>
    <div class="gh-head-menu">
            {{navigation}}
    </div>
</header>

<hr>

<main>

    {{{body}}}
    {{!-- All content gets inserted here, index.hbs, post.hbs, etc --}}

</main>

{{!-- <foot> --}}

<footer>
    <hr>
    <div class="foot">
        <div class="copyright">
            <a class="" href="{{@site.url}}">
                {{@site.title}}
            </a> © 2021
        </div>
        <div class="info">
            Published with <a href="https://ghost.org" target="_blank" rel="noopener">Ghost</a> <br>
            Theme based on <a href="https://latex.now.sh/" target="_blank" rel="noopener">LaTeX.css</a>
        </div>
    </div>
</footer>

<script defer src="{{asset "js/katex.min.js"}}"></script>
<script defer src="{{asset "js/auto-render.min.js"}}"
        onload="renderMathInElement(document.body);"></script>
<script defer src="{{asset "js/copy-tex.min.js"}}"></script>
<script defer src="{{asset "js/prism.js"}}"></script>
{{ghost_foot}}
{{!-- Outputs important scripts - should always be included before closing body tag --}}

</body>
</html>
