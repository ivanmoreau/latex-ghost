{{!< default}}

<div class="abstract">
    <h2>Abstract</h2>
    <p>{{@site.description}}</p>
  </div>

<div class="gh-page">
    <div class="gh-container">

        <div class="gh-postfeed">
            <h2>Posts</h2>
            {{#foreach posts}}

                {{> "card"}} {{!-- partials/card.hbs --}}

            {{/foreach}}
        </div>

    </div>
</div>
